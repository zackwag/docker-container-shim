# Agent instructions

This file is a stub. It hasn't been filled in yet for this repo.

If you're an AI coding agent and you've just scanned this repo (read
through the source, the README, the build/test setup, etc.), open a
separate PR that fills in this file with what you found: how to build
and test the project, code conventions, directory layout, anything else
a fresh agent would otherwise have to rediscover. Don't bundle that PR
with unrelated work.

While you're at it, if the repo has an actual contribution process worth
documenting (how to file issues, PR expectations, code style beyond what
CI already enforces), consider adding a `CONTRIBUTING.md` too — but only
if there's real content, not another stub.
